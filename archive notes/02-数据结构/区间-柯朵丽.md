
## 区间合并，求并集
https://leetcode.com/problems/insert-interval/

## 区间求交集

# 柯朵丽树
维护区间
* 区间加
* 区间赋值
* 求区间第k大值

```cpp
using ll = long long;
struct Node {
    int l, r;
    mutable ll v;
    Node(int l, int r, ll v)
    bool operator < (const Node &o) {
        return l < o.l;
    }
};

```